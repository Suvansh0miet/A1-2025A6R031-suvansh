#include <stdio.h>

int main() {
    int N, num, max;
    scanf("%d", &N);
    scanf("%d", &num);
    max = num;
    for(int i = 1; i < N; i++) {
        scanf("%d", &num);
        if(num > max) {
            max = num;
        }
    }

    printf("%d", max);
    return 0;
}
