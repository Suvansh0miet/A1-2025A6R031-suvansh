#include <stdio.h>
#include <math.h>
int main(){
    float P, T;
    float R ,CI;
    scanf(" %f %f %f", &P, &R, &T);
    CI = P * pow((1 + R/100),T)-P;
    printf("%.2f\n", CI);
    
    
    return 0;
}
